apiVersion: apps/v1
kind: Deployment
metadata:
  name: logoutput-dep
spec:
  replicas: 1
  selector:
    matchLabels:
      app: logoutput
  template:
    metadata:
      labels:
        app: logoutput
    spec:
      volumes:
        - name: random-string
          emptyDir: {}
      containers:
        - name: write
          image: vilkorppi/logoutput:write
          volumeMounts:
            - name: random-string
              mountPath: /usr/src/app/random
        - name: read
          image: vilkorppi/logoutput:read
          volumeMounts:
            - name: random-string
              mountPath: /usr/src/app/random
          ports:
            - containerPort: 3000